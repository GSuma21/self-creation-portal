import { ArrayContainsAllDirective } from './permission.directive';

describe('PermissionDirective', () => {
  it('should create an instance', () => {
    // const directive = new HasPermissionDirective(TemplateRef\,'');
    // expect(directive).toBeTruthy();
  });
});
